README.md
scratch/
ignored_*

{{ if not (eq .chezmoi.os "windows") }}
AppData/
Documents/
{{ end }}
{{ if not (eq .chezmoi.os "linux") }}
.bashrc
.zshrc
.profile

.tmux.conf
.local/share/tmux-power

dot_config/systemd
{{ end }}
# .oh-my-zsh/cache

{{ if or 
  (not (lookPath "npiperelay.exe"))
  (eq .chezmoi.os "windows")
  (not (stat "/proc/sys/fs/binfmt_misc/WSLInterop"))
}}
.local/share/wsl-ssh-agent
.config/systemd/user/ssh-agent-pipe.service
{{ end }}